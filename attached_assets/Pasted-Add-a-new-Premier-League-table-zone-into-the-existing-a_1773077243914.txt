Add a new Premier League table zone into the existing app without removing or breaking any current functionality. Do not remove any existing displayed information, routes, components, styling, data sources, or features. This must be an additive change only.

Goal:
Create a new football data zone in the app that displays the current Premier League table, sourced server-side from the Premier League standings JSON endpoint, normalised through our own backend API, cached, and rendered in the frontend in a clean table layout.

Requirements

1. Backend data module
Create a new backend module for Premier League table data.

Use Node.

Add a module similar to:
- lib/premierLeagueTable.js

This module must:
- Fetch the Premier League standings from:
  https://sdp-prem-prod.premier-league-prod.pulselive.com/api/v5/competitions/8/seasons/{season}/standings
- Support automatic season detection
- Default to season = "auto"
- Detect season by year:
  - August to December = current year
  - January to July = previous year
- Include a configurable cache TTL
- Cache results in memory
- Use stale cache fallback if upstream fails
- Use fetch with timeout and AbortController
- Return a normalised JSON structure
- Export:
  - inferPremierLeagueSeasonYear()
  - getPremierLeagueTable()
  - clearPremierLeagueCache()
  - createPremierLeagueTableHandler()

2. Backend API route
Add a new backend API route:
- /api/data/football/premier-league/table

This route must:
- Use the new handler from the module
- Accept optional query parameter:
  - ?season=2025
- Default to auto season if none supplied
- Return JSON only
- Set Cache-Control headers
- Return 502 JSON error if upstream fails and there is no stale cache

3. Normalised JSON format
The backend must return this shape:

{
  "source": {
    "provider": "Premier League site data",
    "documentedApi": false,
    "page": "https://www.premierleague.com/en/tables/premier-league/2025-26/all-matchweeks"
  },
  "competition": {
    "id": 8,
    "name": "Premier League"
  },
  "season": {
    "yearStart": 2025,
    "label": "2025/26"
  },
  "matchweek": 28,
  "live": false,
  "updatedAt": "ISO date string",
  "table": [
    {
      "position": 1,
      "team": {
        "id": 12,
        "name": "Arsenal",
        "shortName": "Arsenal",
        "abbr": "ARS",
        "slug": null,
        "badge": "/assets/football/badges/arsenal.png",
        "badgeMeta": {
          "code": "ars",
          "slug": null
        }
      },
      "played": 28,
      "won": 20,
      "drawn": 5,
      "lost": 3,
      "goalsFor": 60,
      "goalsAgainst": 24,
      "goalDifference": 36,
      "points": 65,
      "form": "WWDWW",
      "qualification": {
        "band": "champions-league",
        "label": "UEFA Champions League"
      }
    }
  ],
  "cache": {
    "hit": false,
    "stale": false,
    "seasonKey": 2025
  }
}

4. Qualification / status logic
Add position classification logic:
- Positions 1 to 4 = champions-league
- Position 5 = europa-league
- Position 6 = conference-league
- Positions 18 to 20 = relegation
- Others = none

Return this inside:
qualification: { band, label }

5. Team badges
Add support for badge URLs using a local mapping file.

Create:
- lib/teamBadges.js or equivalent

Use team abbreviation keys such as:
ARS, AVL, BOU, BRE, BHA, CHE, CRY, EVE, FUL, IPS, LEI, LIV, MCI, MUN, NEW, NFO, SOU, TOT, WHU, WOL

Map each one to a local asset path like:
- /assets/football/badges/arsenal.png
- /assets/football/badges/liverpool.png

If assets do not yet exist, still wire the mapping in so the system is ready. Do not fail if a badge is missing. Use null fallback.

6. Frontend zone
Add a new frontend zone/component for the Premier League table.

Requirements:
- Create a new UI block/zone called:
  Premier League Table
- It must fetch data from:
  /api/data/football/premier-league/table
- Render:
  - position
  - badge if available
  - team name
  - played
  - won
  - drawn
  - lost
  - goals for
  - goals against
  - goal difference
  - points
- Show loading state
- Show error state
- Handle missing badge gracefully
- Do not break existing layout
- Make the component fit the app’s current design language and styling
- Use existing UI framework and conventions already used in the app
- Reuse current table/card styling patterns if they exist

7. Row styling / visual indicators
Add row styling hooks or classes based on qualification.band:
- champions-league
- europa-league
- conference-league
- relegation
- none

Do not hardcode ugly colours unless the app already has design tokens. Prefer existing design system variables or subtle border/indicator styling.

8. New app section / navigation
Integrate the new zone into the existing app in a sensible place.

Requirements:
- Add a new football or sports section if one already exists
- If not, add a new zone entry labelled:
  Premier League Table
- Ensure it is reachable in the UI
- Do not remove or rename existing sections
- Do not disturb current navigation behaviour

9. Code quality and safety
Important:
- Preserve all existing functionality
- Do not delete any code unless absolutely necessary for integration
- Do not remove any current displayed information
- Do not replace unrelated components
- Check imports, exports, file paths, and route registration carefully
- Ensure the full implementation is complete and working
- If the project uses ES modules, keep ES modules
- If the project uses CommonJS, adapt appropriately
- Match the existing project structure and style
- Add comments in the code where useful
- Ensure no linting or syntax errors are introduced

10. Suggested file additions
Add or adapt files like these if appropriate to the existing structure:
- lib/premierLeagueTable.js
- lib/teamBadges.js
- routes/football.js
- frontend component for Premier League table zone
- any necessary asset folder references

11. Backend implementation details
Please implement:
- buildStandingsUrl()
- buildTablePageUrl()
- safeNumber()
- classifyPosition()
- normaliseEntry()
- normaliseStandingsPayload()
- fetchJson() with timeout
- in-memory cache store using Map
- stale fallback support

12. Frontend implementation details
Please implement:
- data fetch hook or loader
- loading UI
- error UI
- table renderer
- badge image rendering
- qualification band row styling
- responsive layout if the app already supports responsive zones

13. Final output required
After making the changes:
- Show me all files created or changed
- Show the important code snippets
- Explain where the new zone was added in the UI
- Explain the new API route
- Confirm that existing functionality was preserved
- Point out any badge assets that still need to be supplied manually

Please now inspect the current codebase structure and implement this feature in the most compatible way for the existing Vectormesh app, rather than forcing a generic structure.