Add a new live-data zone called SpaceX Launch Countdown into the existing app without removing, replacing, or breaking any current functionality. This must be an additive change only. Do not remove any current displayed information, routes, components, styling, zones, cards, or navigation items.

Goal:
Create a new SpaceX Launch Countdown zone powered by a backend integration with the SpaceX API, exposed through our own backend API route and rendered in the frontend using the app’s existing design patterns.

Important implementation rule:
Inspect the current codebase first and follow the existing project structure, routing style, naming conventions, data-fetching approach, component layout, and styling system. Do not force a generic architecture if the project already has one.

General requirements

- Preserve all existing working functionality
- Do not remove or rename any existing sections unless absolutely necessary
- Reuse existing API route patterns if present
- Reuse existing UI cards, tables, zone wrappers, filters, tabs, or layout components where possible
- Keep upstream API calls on the backend only
- Frontend must call our own backend routes only
- Handle null/missing fields safely
- Add loading, error, and empty states
- Use short in-memory caching
- Ensure no syntax or linting errors are introduced

==================================================
1. BACKEND SPACEX INTEGRATION
==================================================

Use the public SpaceX API.

Create or adapt backend files such as:
- lib/spaceXLaunch.js
- routes/spacex.js

Use Node and match the existing module system in the project (ESM or CommonJS).

Suggested environment variables:
- SPACEX_BASE_URL
- SPACEX_TIMEOUT_MS
- SPACEX_CACHE_TTL_MS

Default values if env vars are not set:
- SPACEX_BASE_URL = https://api.spacexdata.com/v4
- SPACEX_TIMEOUT_MS = 10000
- SPACEX_CACHE_TTL_MS = 60000

Important:
- Use the current SpaceX API approach
- Do not use deprecated older API patterns
- Keep all upstream-specific assumptions isolated inside one service file
- Build defensively in case some launch fields are null

Implement helper functions such as:
- buildSpaceXUrl()
- fetchJsonWithTimeout()
- normaliseNextLaunch()
- normaliseSpaceXPayload()
- createCacheKey()
- clearSpaceXCache()
- getNextSpaceXLaunch()
- createNextSpaceXLaunchHandler()

Use fetch with:
- AbortController
- timeout handling
- defensive null checks

Use in-memory cache via Map with:
- configurable TTL
- stale fallback if upstream refresh fails
- cache metadata returned in the JSON response

==================================================
2. UPSTREAM REQUEST SHAPE
==================================================

Fetch the next upcoming launch from the SpaceX API.

Use a backend request shape compatible with the current SpaceX launch endpoints.
Prefer a dedicated “next launch” upstream route if available in the current API.
If the codebase already has a generic fetch wrapper, integrate with that.

If useful, also support optional enrichment lookups for related entities such as:
- rocket
- launchpad

But keep the first implementation lightweight and resilient.

==================================================
3. BACKEND ROUTE
==================================================

Create or adapt a backend route such as:

- /api/data/spacex/next-launch

This route must:
- return JSON only
- use our SpaceX service module
- set Cache-Control headers
- return readable JSON errors
- return stale cached data with a stale flag if refresh fails and stale data exists
- return 502 JSON error if upstream fails and there is no cache to fall back to

==================================================
4. NORMALISED RESPONSE SHAPE
==================================================

Normalise the backend response into a stable frontend-friendly structure like this:

{
  "source": {
    "provider": "SpaceX API",
    "documentedApi": true
  },
  "updatedAt": "ISO date string",
  "launch": {
    "id": "launch-id",
    "name": "Starlink 10-12",
    "net": "2026-03-21T22:30:00.000Z",
    "upcoming": true,
    "flightNumber": 250,
    "details": "Mission summary if present",
    "success": null,
    "rocketId": "rocket-id",
    "launchpadId": "launchpad-id",
    "rocketName": null,
    "launchpadName": null,
    "links": {
      "webcast": null,
      "wikipedia": null,
      "article": null,
      "patchSmall": null,
      "patchLarge": null
    }
  },
  "cache": {
    "hit": false,
    "stale": false
  }
}

Keep the frontend dependent only on this normalised shape, not on raw SpaceX fields.

If enrichment lookups are implemented, populate:
- rocketName
- launchpadName

If not available, return null safely.

==================================================
5. FIELD MAPPING REQUIREMENTS
==================================================

Map common launch fields carefully and defensively.

At minimum support:
- id
- name
- date/net launch time
- upcoming
- flight number
- details
- success
- rocket id
- launchpad id
- links to webcast/article/wikipedia if present
- mission patch images if present

Add comments in the service file noting where payload mappings may need adjustment if the upstream schema changes.

If any date field is returned in a format that needs normalising, convert it to a consistent ISO string for frontend use.

==================================================
6. FRONTEND SPACEX ZONE
==================================================

Add a new frontend zone/component called:

- SpaceX Launch Countdown

Requirements:
- Fetch only from our backend route
- Show mission name
- Show launch time
- Show a live countdown relative to now
- Show details summary if present
- Show loading state
- Show error state
- Show empty state if no launch data is available
- Handle missing fields gracefully
- Fit the app’s current design system and styling
- Reuse existing card/zone/table/list patterns if available

If patch imagery is available, show it using the app’s current image/card style.
If not, do not fail.

If webcast/article/wikipedia links are available and the app already has an external-link pattern, render them consistently with the rest of the UI.

==================================================
7. COUNTDOWN BEHAVIOUR
==================================================

Implement a frontend countdown timer that:
- updates on the client every second
- does not trigger a backend refetch every second
- counts down to the normalised launch.net field
- handles past times gracefully if the launch has just passed or data changes unexpectedly

For example:
- “Launch in 3d 04h 12m 08s”
- or “Launch time passed” if the time is already in the past

Keep the countdown lightweight and consistent with the existing frontend style.

==================================================
8. UI PLACEMENT
==================================================

Integrate the SpaceX Launch Countdown zone into the most sensible existing place in the app.

Requirements:
- If there is already a dashboard, utilities, live data, inspiration, or information wall section, place it there
- Otherwise add a clearly labelled new zone entry called SpaceX Launch Countdown
- Do not disturb existing navigation behaviour
- Do not remove or rename existing sections

==================================================
9. SHARED QUALITY REQUIREMENTS
==================================================

- Do not log secrets
- Keep logs minimal and useful
- Return clean JSON errors from backend
- Guard against null/undefined payload fields
- Do not crash on partial upstream responses
- Keep code comments where they help future maintenance
- Match the project’s existing coding conventions
- Ensure imports, exports, and route registration are complete

==================================================
10. FINAL OUTPUT REQUIRED
==================================================

After implementing:
- list all files created or changed
- show the important code snippets
- explain where the new SpaceX Launch Countdown zone was added in the UI
- explain the new backend route
- explain any environment variables used
- confirm that no existing functionality was removed
- point out any SpaceX payload assumptions that may need tuning after live testing

Important delivery instruction:
Implement the feature fully, not as placeholders.
Wire the backend service module, route, and frontend zone together so the SpaceX Launch Countdown zone is actually usable in the app.