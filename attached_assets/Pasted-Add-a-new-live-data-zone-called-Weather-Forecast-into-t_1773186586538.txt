Add a new live-data zone called Weather Forecast into the existing app without removing, replacing, or breaking any current functionality. This must be an additive change only. Do not remove any current displayed information, routes, components, styling, zones, cards, or navigation items.

Goal:
Create a new Weather Forecast zone powered by a backend integration with Open-Meteo, exposed through our own backend API route and rendered in the frontend using the app’s existing design patterns.

Important implementation rule:
Inspect the current codebase first and follow the existing project structure, routing style, naming conventions, data-fetching approach, component layout, and styling system. Do not force a generic architecture if the project already has one.

General requirements

- Preserve all existing working functionality
- Do not remove or rename any existing sections unless absolutely necessary
- Reuse existing API route patterns if present
- Reuse existing UI cards, tables, zone wrappers, filters, tabs, or layout components where possible
- Keep upstream API calls on the backend only
- Frontend must call our own backend routes only
- Handle null/missing fields safely
- Add loading, error, and empty states
- Use short in-memory caching
- Ensure no syntax or linting errors are introduced

==================================================
1. BACKEND WEATHER INTEGRATION
==================================================

Use Open-Meteo as the upstream source.

Create or adapt backend files such as:
- lib/openMeteoWeather.js
- routes/weather.js

Use Node and match the existing module system in the project (ESM or CommonJS).

Suggested environment variables:
- OPEN_METEO_BASE_URL
- WEATHER_DEFAULT_LAT
- WEATHER_DEFAULT_LON
- WEATHER_DEFAULT_NAME
- WEATHER_TIMEOUT_MS

Default values if env vars are not set:
- OPEN_METEO_BASE_URL = https://api.open-meteo.com/v1
- WEATHER_DEFAULT_LAT = 51.5072
- WEATHER_DEFAULT_LON = -0.1276
- WEATHER_DEFAULT_NAME = London
- WEATHER_TIMEOUT_MS = 10000

Implement helper functions such as:
- buildWeatherUrl()
- fetchJsonWithTimeout()
- normaliseCurrentWeather()
- normaliseDailyForecast()
- normaliseWeatherPayload()
- createCacheKey()
- clearWeatherCache()
- getWeatherForecast()
- createWeatherForecastHandler()

Use fetch with:
- AbortController
- timeout handling
- defensive null checks

Use in-memory cache via Map with:
- configurable TTL
- stale fallback if upstream refresh fails
- cache metadata returned in the JSON response

==================================================
2. OPEN-METEO REQUEST SHAPE
==================================================

Build the backend request to Open-Meteo using:
- latitude
- longitude
- timezone
- current weather fields
- daily forecast fields

Use a sensible request for:
- current temperature
- current wind speed
- current wind direction
- current weather code
- daily max/min temperature
- daily precipitation probability max
- daily weather code

Support query params on our backend route where sensible:
- lat
- lon
- locationName
- timezone
- days

If none are provided, default to London.

==================================================
3. BACKEND ROUTE
==================================================

Create or adapt a backend route such as:

- /api/data/weather/forecast

This route must:
- return JSON only
- use our weather service module
- set Cache-Control headers
- return readable JSON errors
- return stale cached data with a stale flag if refresh fails and stale data exists
- return 502 JSON error if upstream fails and there is no cache to fall back to

==================================================
4. NORMALISED RESPONSE SHAPE
==================================================

Normalise the backend response into a stable frontend-friendly structure like this:

{
  "source": {
    "provider": "Open-Meteo",
    "documentedApi": true
  },
  "location": {
    "name": "London",
    "latitude": 51.5072,
    "longitude": -0.1276,
    "timezone": "Europe/London"
  },
  "updatedAt": "ISO date string",
  "current": {
    "time": "2026-03-10T11:00:00Z",
    "temperatureC": 9.4,
    "windSpeedKph": 14,
    "windDirection": 220,
    "weatherCode": 3
  },
  "daily": [
    {
      "date": "2026-03-10",
      "tempMaxC": 11,
      "tempMinC": 5,
      "precipitationProbabilityMax": 30,
      "weatherCode": 3
    }
  ],
  "cache": {
    "hit": false,
    "stale": false
  }
}

Keep the frontend dependent only on this normalised shape, not on raw Open-Meteo fields.

==================================================
5. FRONTEND WEATHER ZONE
==================================================

Add a new frontend zone/component called:

- Weather Forecast

Requirements:
- Fetch only from our backend route
- Show current temperature
- Show wind speed
- Show wind direction
- Show a 3 to 5 day forecast
- Show loading state
- Show error state
- Show empty state if no data
- Handle missing fields gracefully
- Fit the app’s current design system and styling
- Reuse existing card/zone/table/list patterns if available

If the app already uses icons or visual badges, map weatherCode values to existing icon patterns.
If no icon system exists, render clean text labels and keep it lightweight.

If appropriate to the existing app, display:
- current summary at top
- forecast cards or rows underneath

==================================================
6. WEATHER CODE HANDLING
==================================================

Create a lightweight weather code mapping utility if useful.

At minimum, support readable labels for common Open-Meteo weather codes such as:
- clear
- partly cloudy
- cloudy
- fog
- drizzle
- rain
- snow
- thunderstorm

If the current app already has an icon or badge system, integrate with that.
If not, use readable text and avoid introducing a heavy dependency.

==================================================
7. UI PLACEMENT
==================================================

Integrate the Weather Forecast zone into the most sensible existing place in the app.

Requirements:
- If there is already a dashboard, utilities, live data, venue, or environment section, place it there
- Otherwise add a clearly labelled new zone entry called Weather Forecast
- Do not disturb existing navigation behaviour
- Do not remove or rename existing sections

==================================================
8. SHARED QUALITY REQUIREMENTS
==================================================

- Do not log secrets
- Keep logs minimal and useful
- Return clean JSON errors from backend
- Guard against null/undefined payload fields
- Do not crash on partial upstream responses
- Keep code comments where they help future maintenance
- Match the project’s existing coding conventions
- Ensure imports, exports, and route registration are complete

==================================================
9. FINAL OUTPUT REQUIRED
==================================================

After implementing:
- list all files created or changed
- show the important code snippets
- explain where the new Weather Forecast zone was added in the UI
- explain the new backend route
- explain any environment variables used
- confirm that no existing functionality was removed
- point out any Open-Meteo payload assumptions that may need tuning after live testing

Important delivery instruction:
Implement the feature fully, not as placeholders.
Wire the backend service module, route, and frontend zone together so the Weather Forecast zone is actually usable in the app.